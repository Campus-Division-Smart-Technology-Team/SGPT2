# Core dependencies for AskAlfred chatbot
streamlit==1.49.1
openai>=1.0.0
pinecone>=7.3.0
python-dotenv>=1.0.0

# AWS S3 integration for ingestion scripts
boto3>=1.34.0
botocore>=1.34.0

# Document processing
pypdf>=4.0.0          # PDF extraction (replaces PyPDF2)
python-docx>=1.1.0    # DOCX extraction
pandas>=2.0.0         # CSV processing and data manipulation

# Text processing and embeddings
tiktoken>=0.5.0       # Token counting for chunking

# Optional but recommended for production
requests>=2.31.0      # HTTP requests (may be used by dependencies)
urllib3>=2.0.0        # URL handling (dependency of requests/boto3)